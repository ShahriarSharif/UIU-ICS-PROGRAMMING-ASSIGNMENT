#include <stdio.h>

int main()
{
    int first_num, middle_num, last_num;

    scanf("%d %d %d", &first_num, &middle_num, &last_num);

    printf("First Value = %d,  Last Value = %d", first_num, last_num);

    return 0;
}